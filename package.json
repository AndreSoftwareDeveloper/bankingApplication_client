{
  "name": "bankingApplication_Client",
  "version": "1.0.0",
  "description": "Monorepo containing source code of mobile app created using Angular and Ionic and built APK",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "publish": "lerna publish"
  }
}
