export interface Customer {
  address: string;
  birthDate: string;
  birthPlace: string;
  customerNumber: number;
  email: string;
  firstName: string;
  id: number;
  idCardNumber: string;
  lastName: string;
  nip: string | null;
  password: string;
  pesel: string;
  phoneNumber: string;
  regon: string | null;
  verificationToken: number;
}
